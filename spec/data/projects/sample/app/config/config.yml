# include other config files
imports:
    - { resource : parameters.yml }
    - { resource : bundles.yml }

framework:

    # application settings
    app:
        host: "%app.http.host%"
        port: "%app.http.port%"
