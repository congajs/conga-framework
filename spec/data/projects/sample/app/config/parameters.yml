parameters:

    app.http.port: 3000
    app.http.host: localhost

    from.env: "$ENV{ENV_TEST, 'bar'}"
    default.env: "$ENV{ENV_DOESNT_EXIST, 'use this default'}"
    hello: there
    another.env: "$ENV{ENV_TEST_2, '1234'}"
