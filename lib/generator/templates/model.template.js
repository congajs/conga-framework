/**
 * This file was generated by @conga/framework
 */

/**
<% _.forEach(metadata.definition.annotations, (annotation) => {%> * <%= annotation %>
<% }) %> */
module.exports = class <%= metadata.name %> {
<% _.forEach(metadata.statics, (s) => { %>
    /**
     * <%= s.property %> enum values
     */
    static get <%= s.name %>() {
        return <%= JSON.stringify(s.value) %>;
    }
<% }) %>
    /**
     * @constructor
     */
    constructor() {
<% _.forEach(metadata.properties, (property) => { %>
        /**
<% _.forEach(property.annotations, (annotation) => {%>         * <%= annotation %>
<% }) %>         */
        this.<%= property.property %> = <%= property.defaultValue %>;
<% }) %>
    }
}
